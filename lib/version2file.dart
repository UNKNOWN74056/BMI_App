import 'package:flutter/material.dart';

class Version2file extends StatelessWidget {
  const Version2file({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}